echo "Running scripts under githooks"
if [ -d .githooks/pre-commit.d ]; then
    for i in .githooks/pre-commit.d/*; do
      if [ -r $i ]; then
        $i
      fi  
    done
    unset i 
else
    echo "File not found"
fi
